import { ComingSoon } from "@/components/coming-soon";

export default function VendorsPage() {
  return (
    <ComingSoon
      title="Vendor Tracker"
      description="Vendor contacts and per-tournament commitment status are coming in an upcoming update."
      issue={5}
    />
  );
}
